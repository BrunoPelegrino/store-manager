const productsModel = require('./products.model');
const createProductModel = require('./createProduct.model');
const saleModel = require('./sales.model');

module.exports = {
  productsModel,
  createProductModel,
  saleModel,
};